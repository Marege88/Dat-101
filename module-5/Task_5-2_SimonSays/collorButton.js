"use strict";
import {TSpriteButton } from "libSprite";

export class TColorButton {
    #collorButtons;
    constructor (aSpcvs, aSPI){
        super(aSpcvs, aSPI, aSPI.dst.x, aSPI.dst.y);
    }
    
}